@require "../../variables"
@require "../../set"

.ServerSubscriptions
    // "Support your server with subscriptions"
    &SectionMemberUpsell-container,
    // "Become the first subscriber"
    &RecentActivityFeed-empty-wrapper
        @extends $ContentMildlyBordered-2
    // Copper, silver, gold
    &TierUpsellCard
        &-container
            @extends $FullBorderLight-0
        &-header
            @extends $Panel-2
        &-icon-lock
            @extends $SvgDarker
        &-body
            @extends $Panel-3

// Points
.IconBulletedList
    &-icon
        @extends $SvgDark