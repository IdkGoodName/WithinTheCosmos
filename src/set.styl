@require "./variables"

// Sets both color and fill
font-color(colour)
    color: colour
    fill: colour

// For setting transition in a lot of places
transition(prop, fn = ease-out)
    transition: prop $transition-period fn
transition-multiple()
    transition: $transition-period ease-out
    transition-property: arguments